[project]
name = "poweriq-prometheus"
version = "1.0.0"
description = "Prometheus exporter for Sunbird PowerIQ"
requires-python = ">=3.11"
dependencies = [
    "prometheus_client>=0.21,<1.0",
    "requests>=2.31,<3.0",
]

[project.scripts]
poweriq-exporter = "poweriq_exporter.main:main"
